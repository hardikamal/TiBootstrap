<Alloy>
    <Widget id="drawer" src="nl.fokkezb.drawer">
        <!-- Menu -->
        <Window role="leftWindow">
            <Widget src="f.Menu" />
        </Window>
        <!-- Navigation Window -->
        <NavigationWindow role="centerWindow">
            <Window>
                <LeftNavButton>
                    <Widget src="f.MenuButton" onClick="toggleMenu" id="menu"  />
                </LeftNavButton>
                <Require type="view" src="stream" id="Stream"/>
            </Window>
        </NavigationWindow>
    </Widget>
</Alloy>